# connect-pc-mobile-claude — 브레인스토밍 기록

> 작성일: 2026-06-13
> 상태: **✅ 설계 승인 완료** — 정식 설계 문서로 이관됨
> 👉 **다음에 들어오면 이 문서부터 읽기:** `docs/superpowers/specs/2026-06-13-connect-pc-mobile-claude-design.md`
> 이 파일은 초기 브레인스토밍 히스토리(아카이브). 확정 내용·다음 단계는 위 설계 문서에 있음.
>
> **확정 요약:** 방식 A / 로컬도구=(가)CLI / 미리보기=(다)Firebase / Claude=기존 Claude Code 재사용 / 페어링=코드·QR / 수익화=구독제(MVP는 seam만)

---

## 한 줄 요약
**"폰에서 Claude한테 시켜서 앱 만들고, 폰에서 URL로 미리보며 피드백하는" 걸 누구나 할 수 있게 해주는 플랫폼.** 실행은 각 사용자 PC + 각자 Claude 계정에서 → 원가 낮고 수익화 가능.

## 왜 이 방향인가 (핵심 결정)
- 클라우드에서 사용자 코드를 직접 돌리면(샌드박스) 제일 비싸고 위험함 → **버림**
- 대신 **사용자가 자기 PC에서 내 도구를 실행** → 무거운 작업(코드 실행/빌드/AI비용)이 전부 사용자 쪽
- 너(운영자)는 **얇은 중계 서버**만 운영 → 싸고 안전, 마진 좋음
- AI 요금도 사용자가 자기 Claude 계정으로 부담 → 운영 원가 ≈ 0

## 전체 구조
```
[폰: 웹앱]  ──명령──▶  [중계 서버]  ──전달──▶  [사용자 PC: 로컬 도구]
   ▲                                                  │
   │                                          Claude로 코드 생성
   └────────── 미리보기 화면 ◀──── 배포/터널 ◀────────┘
```

### 3개 구성요소
1. **로컬 도구** (사용자 PC 설치)
   - Claude Code / Claude Agent SDK 감싸서 실행
   - 채팅 명령 받기 → 코드 생성 → 빌드 → 배포
   - 중계 서버와 지속 연결(WebSocket) 유지
2. **중계 서버** (운영자가 호스팅) ← **수익화 포인트**
   - 로그인 / 결제 / 라이선스
   - 폰 ↔ PC 메시지 중계 (코드 실행 안 함, 전달만)
3. **폰 웹앱** (PWA)
   - 채팅으로 명령
   - 폰 크기 프레임으로 미리보기
   - 상태/로그 표시

## 확정된 결정
- [x] 방식 **A**: 폰에서 **명령 + 미리보기 둘 다** (완성형 모바일 루프)
- [x] 실행은 사용자 PC에서, 중계 서버는 얇게
- [x] 작업 폴더: `~/projects/connect-pc-mobile-claude`

## 아직 확정 안 된 선택 (다시 와서 정할 것)
- [ ] **선택1 — 로컬 도구 형태**
  - (가) CLI/데몬 ← *추천 (MVP 최단거리)*
  - (나) 데스크탑 앱 (Tauri/Electron, 일반인 친화적, 개발량↑)
- [ ] **선택2 — 미리보기 방식**
  - (다) Firebase 배포 후 URL 보기 ← *추천 (단순, 기존 Firebase 활용)*
  - (라) 로컬 개발서버 터널링 (실시간 라이브, 중계서버에 터널기능 필요)

> **잠정 추천 조합: (가) CLI + (다) Firebase 배포** = 가장 빨리 작동하는 MVP

## 알려진 난관 / 주의점
- "폰으로 시키고 PC에서 실행"하려면 사용자 PC가 **켜져 있고 중계서버에 연결**돼 있어야 함 → 이 연결이 제품의 심장
- 사용자 인증/Claude 계정 연동 방식 (각자 API 키? 구독 로그인?) 정해야 함
- 보안: 중계 서버는 메시지만 전달하고 코드는 안 보게 설계 (프라이버시)

## 다음 단계 (여기서부터 이어가기)
1. 위 **선택1, 선택2** 확정 (잠정 추천대로 갈지 결정)
2. 확정되면 → 정식 설계 문서 작성: `docs/superpowers/specs/2026-06-13-connect-pc-mobile-claude-design.md`
3. 설계 승인 후 → 구현 계획(writing-plans) → 개발 시작
4. MVP 범위 정의: 우선 "로컬 도구 ↔ 중계 ↔ 폰" 최소 연결 1개 동작시키기

## 참고 (기술 부품 후보)
- **Claude Agent SDK** — 채팅받아 Claude로 코드 시키는 "두뇌"를 프로그램으로 구현
- **샌드박스 서비스** (E2B, Daytona) — *이 방향에선 불필요*, 클라우드 실행 갈 때만
- **터널** — 선택2의 (라) 갈 때 (ngrok류 또는 자체 구현)
- **배포** — Firebase Hosting (기존 자산 활용)
