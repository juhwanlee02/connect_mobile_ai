# 폰 UX — md 편집·컴팩트 진행률·쉬운 말 (계획 ④/④, 경량 프로세스)

> 경량 실행: 태스크별 리뷰 생략, 태스크 순차 구현(각자 npm test+tsc 게이트) → 최종 전체 리뷰 1회 → 스모크 → 머지. 근거: UI·문구 중심의 저위험 변경 + 사용자 시간 제약.

**Goal:** ① 산출물 md를 폰에서 직접 수정 ② 진행률 UI를 접이식 한 줄로(채팅 시야 확보) ③ 산출물·보고의 내부 용어를 일반 사용자 눈높이로.

**Spec:** `2026-07-11-login-onboarding-ux-design.md` §4·§5·§6 (+ 계획 ③ 최종 리뷰 이월 follow-up 4건 동봉).

## Global Constraints
- 원자적 쓰기(tmp+rename), 한국어, npm test+tsc 클린, 커밋 태스크별(Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>).
- artifact_set 규칙(스펙 §4): artifacts에 등록된 `.md` 키만, 경로 탈출 차단(artifact_get과 동일), **실행 중(manager.isRunning) 거부**, 1MB 상한, 저장 후 채팅 로그 `📝 사용자가 <라벨> 문서를 직접 수정했어요` + 갱신본 artifact 회신.
- 컴팩트 헤더(스펙 §5): 접힘 기본 44px 이하 한 줄(진행바+`N/M 라벨·상태`+▾), awaiting_confirm일 때만 그 줄에 [✓ 컨펌] 인라인, 탭하면 기존 카드 전체(스텝 pill·중단·산출물) 펼침, 상태 전이로 자동 펼치지 않음, 접힘 상태 프로젝트별 localStorage.
- 쉬운 말(스펙 §6): 계약 7항은 기존(계획 ①에서 신설) — 이번엔 각 builtin 스킬의 "채팅 보고" 형식에 반영 + ideation의 "실행 정보" 표를 산출물 하단 "자세히(감사 로그)" 섹션으로 이동 지시 + 용어 첫 등장 풀이 지시.

## Task 1: artifact_set 백엔드 + 계획③ 이월 수정
- `src/shared/protocol.ts`: `ArtifactSetMsg { type:"artifact_set"; project; key; content }` (PhoneOutbound), 응답은 기존 `artifact`/`log`.
- `src/cli/pipeline-manager.ts` `handleMessage`에 `artifact_set` 케이스 + 메서드:
```ts
  private artifactSet(project: string, key: string, content: string): void {
    const d = this.dir(project);
    if (this.running.has(project)) {
      this.opts.send({ type: "log", project, text: "⚠️ 작업이 끝난 뒤 수정해 주세요 (Claude가 파일을 쓰는 중이에요)" });
      return;
    }
    if (typeof content !== "string" || Buffer.byteLength(content) > 1024 * 1024) {
      this.opts.send({ type: "log", project, text: "⚠️ 문서가 너무 커요(1MB 제한)" });
      return;
    }
    const state = readPipelineState(d);
    const rel = state?.artifacts[key];
    if (!rel || !rel.endsWith(".md")) return;
    const base = normalize(d);
    const path = normalize(join(d, rel));
    if (!(path === base || path.startsWith(base + sep))) return;
    try {
      const tmp = `${path}.tmp-${process.pid}`;
      writeFileSync(tmp, content);
      renameSync(tmp, path);
    } catch {
      this.opts.send({ type: "log", project, text: "⚠️ 저장에 실패했어요" });
      return;
    }
    this.opts.send({ type: "log", project, text: `📝 사용자가 문서를 직접 수정했어요 (${rel})` });
    this.opts.send({ type: "artifact", project, key, content });
  }
```
(임포트: writeFileSync/renameSync 추가. host.ts의 project 검증 목록에 `artifact_set` 추가 — artifact_get과 동일 사전 검증 4종 블록에 편입.)
- 계획③ 이월: `src/server/setup-api.ts` 정적 2개 `readFileSync`를 try/catch(실패 시 500 fail — 프로세스 사망 방지), 401 응답에 `content-type: text/plain; charset=utf-8`, doc-templates put에 md `## ` 헤더 부재 시에도 저장하되 응답에 `warning` 필드(클라 토스트), README env-우선 캐비앳 1줄.
- 테스트: pipeline-manager.test.ts에 artifact_set 4케이스(정상 저장+로그+artifact 회신 / 실행 중 거부 / 미등록 키 무시 / 경로 탈출 무시), setup-api.test.ts에 정적 자산 부재 500·경고 필드.

## Task 2: 폰 웹 — md 편집 + 컴팩트 진행 헤더
- **md 편집**: 뷰어(md kind)에 [✏️ 직접 수정] 버튼 → `#viewerMd` 대신 전체폭 textarea 토글(+[저장]/[취소]) → 저장 시 `send({type:"artifact_set", ...})`, 도착하는 `artifact` 메시지가 기존 applyMessage 경로로 뷰어 갱신(편집 모드 해제). 실행 중 거부 log는 기존 채팅/토스트로 보임 — 편집 내용은 textarea에 유지(닫지 않음).
- **컴팩트 헤더**(index.html+app.js+store.js):
  - `#stepper`·`#stageCard`를 감싸는 접이식 컨테이너: 접힘 시 한 줄 `#stageLine`(미니 진행바+`2/7 PRD · 진행 중`+상태색+▾, awaiting_confirm이면 [✓ 컨펌] 버튼 인라인) — 기존 stageCard의 상시 비활성 "컨펌(단계가 끝나면 활성화)" 버튼 삭제.
  - 탭 → 펼침(기존 stepper+stageCard 그대로 표시, ▴), 재탭 → 접힘. 상태 전이로 자동 펼치지 않음.
  - 접힘/펼침 localStorage `cpmc_stage_open:<project>` 기억. 순수 로직(라벨 문자열 조립 `stageLineText(snap)`)은 store.js에 두고 vitest 테스트.
- 검증: node --check, id 전수 대조, tests/web 통과.

## Task 3: 쉬운 말 — 스킬 문구 + 문서
- `commands/pipeline/pipeline-ideation.md`: 채팅 보고·IDEAS.md 산출 지시에 "실행 정보(검색 횟수 등 감사 로그성 숫자)는 IDEAS.md 최하단 '자세히(만든 과정 기록)' 섹션으로 옮기고, 본문/채팅에는 카드 내용만" + "카드=아이디어 후보 1장, slug=영문 식별자 같은 내부 용어는 첫 등장에 괄호 풀이".
- `pipeline-prd.md`·`pipeline-estimate.md`·`pipeline-release.md`의 "채팅으로 요약 보고" 단락에 공통 문장 추가: "보고는 비개발자 눈높이로 — 전문용어는 풀어 쓰고, 표의 숫자에는 뜻을 한 줄로."(계약 7항 참조 표기).
- README: md 직접 수정·접이식 진행 헤더 소절 2개, ACCEPTANCE 시나리오(편집→저장→뷰어 갱신→컨펌 / 접힘 유지 / 실행 중 편집 거부).

## 완료 게이트
최종 전체 리뷰 1회(fable) → must-fix만 수정 → 라이브 스모크(artifact_set ws 왕복+실행중 거부) → main 머지.
