# 비즈니스 모델 — {{app_title}}

<!-- 이 문서는 비즈니스 모델 단계의 산출물이다. 아래 블록(메타 + §1~§4)의 제목·순서를
     그대로 유지한다(추가·삭제 금지, 해당 없으면 "해당 없음"+이유). 값은 사용자가 폰 설문에서
     제출한 business/answers.json과 1:1로 일치해야 한다. -->

## 메타

| 항목 | 값 |
|---|---|
| slug | {{slug}} |
| 기준 | 아이디어 카드({{idea_slug}}) · 구조(wireframe) |
| 상태 | 컨펌 대기 |
| 제출값 | `business/answers.json` |

## §1 수익화 방식

- **선택**: {{monetization_choice}} <!-- 배너 광고 / 전면·보상형 광고 / 완전 무료 / 구독·인앱결제 -->
- **적용 방식**: <!-- 광고면: AdMob(개발 단계에서 테스트 ID로 배선, 릴리즈 전 실 ID 교체). 무료면: 수익화 없음. 구독이면: 결제·권한 서버 필요 → §3 게이트 참조 -->
- **화면 위치**: <!-- 광고/잠금 UI가 노출될 화면(구조·목업 기준). 없으면 "해당 없음" -->

## §2 데이터 · 백엔드 판정

- **선택**: {{backend_choice}} <!-- 기기 로컬 저장 / 정적 번들 / 서버 동기화(Firebase 등) -->
- **판정**: <!-- 로컬 → 백엔드 없음 / 정적 → 번들 데이터 / 서버 → 백엔드 필요(스택 근거) -->
- **비고**: 이 판정이 4단계 산정(estimate)의 §1 입력이다(산정은 이 값을 재판정하지 않고 소비한다).

## §3 서버비 0원 게이트

- **결과**: <!-- 통과(로컬·정적·광고만 — 서버비/결제 인프라 없음) / 유보(구독 또는 서버 선택 — 아래 TODO로 이월) -->
- **원칙**: 서버·결제가 필요한 선택도 **막지 않는다**. 대신 그 부분을 "나중에 연동"으로 남기고 지금은 스텁으로 개발을 이어간다.

## §4 나중에 연동할 TODO

<!-- §1·§2에서 서버비/결제 인프라가 필요한 선택을 했다면, 개발 단계가 스텁으로 남길 항목을
     구체적으로 나열한다. 전부 0원(로컬+광고/무료)이면 "해당 없음 — 지금 구성으로 완결" 한 줄. -->

| 항목 | 무엇을 스텁으로 두나 | 나중에 무엇을 연동하나 |
|---|---|---|
| <!-- 예: 구독 결제 --> | <!-- 예: 잠금 해제를 항상 열림으로 --> | <!-- 예: 스토어 인앱결제 + 권한 검증 --> |

---

**다음**: 컨펌하면 이 결정을 반영해 PRD를 씁니다 — PRD §6(수익화)는 §1을, 산정(estimate) §1은 §2를 그대로 받습니다.
