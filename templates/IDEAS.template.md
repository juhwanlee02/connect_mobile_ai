<!--
이 템플릿을 채우는 스킬(Phase 4 `/pipeline-ideate`)을 위한 규칙

- 섹션 고정: 아래 "실행 정보"·"후보 카드 목록"·"선택 안내"·"자세히(만든 과정 기록)" 네 섹션의
  제목과 순서를 추가·삭제하지 않는다. 값이 없으면 칸을 비우거나 행을 지우지 말고 "해당 없음"이라고
  명시적으로 적는다.
- 핵심기능 개수: 정확히 3개(MVP 범위)로 명시하여 제시할 것.
- 타겟 열: 표의 "타겟" 열은 한 줄 요약만 적는다 — 타겟·핵심기능의 상세는 짝 HTML 카드(target_persona 등)가
  정본이다. 표는 폰에서 카드를 고르기 위한 요약 리스트일 뿐, 표만 보고 상세를 판단하지 않는다.
- slug 규칙: kebab-case, 영문 소문자로 시작 (예: habit-streak-buddy). 프로젝트 전체·전역 ideas-index.json에서
  중복 판정의 키가 되므로 이 문서 안에서도 서로 겹치면 안 된다.
- 짝 파일 필수: 표의 각 행마다 "HTML 카드" 열에 적힌 경로(`ideas/<NN>-<slug>.html`, NN은 2자리
  zero-padded 순번)에 실제로 templates/proposal.template.html을 채운 파일이 존재해야 한다. 경로 불일치 금지.
- ideas-index.json 쓰기 규약: 제안 확정 시(직접 입력 모드 포함) ideas-index.json에 append하고, 아이디어
  컨펌(채택) 시 adopted에 프로젝트명을 갱신한다. 직접 입력 모드도 인덱스에 남겨야 RELEASE §7 차별점
  대조가 성립한다.
- 완료 시 pipeline.json 등록: 이 파일 자체(예: `IDEAS.md`)의 상대 경로를 `artifacts.ideas` 키로 기록한다.
  개별 HTML 카드는 별도 키로 등록하지 않는다(IDEAS.md 표를 통해서만 참조됨).
- 원자적 쓰기: 임시 파일에 쓴 뒤 rename으로 교체한다. 쓰는 도중의 부분 파일 상태를 그대로 남기지 않는다.
- 재실행 시 기존 카드를 덮어쓰지 않는다: 실행 정보 섹션은 최신 회차로 갱신하고, 카드 표에는 회차 열을
  추가해 이전 회차의 행을 그대로 보존한 채 새 회차 결과를 추가한다. HTML 카드 NN 번호는 전 회차에
  걸쳐 연속 증가한다(회차마다 01부터 다시 시작하지 않는다).

리서치 방법론 6원칙 — 전부 반드시 지키고, 아래 표의 해당 열로 근거를 남길 것:
  1. 양방향 크로스마켓 아비트라지 — 매 실행마다 US→KR, KR→US 두 방향을 모두 조사한다. 한쪽만 조사하고
     끝내는 것은 실패다.
  2. 최신성 — 기준 시점은 실행 당일. 최근 3개월 이내 급부상 사례를 우선하고, 최대 6개월까지만 허용한다.
     근거 자료(기사·랭킹·리뷰)의 게시 날짜를 확인해 6개월을 넘은 스테디셀러는 제외한다.
  3. 인디·MVP 우선 — 대기업·슈퍼앱은 배제한다. 1인/소규모 팀이 수 주 내 MVP를 만들 수 있는 단일 핵심
     기능(scope=small 다수)과 한 문장으로 사로잡는 훅을 우선한다.
  4. 카테고리 다양성 — 최소 5개 이상 서로 다른 카테고리를 포함한다. 어느 한 카테고리도 전체 후보의
     약 1/4을 넘지 않게 균형을 맞춘다.
  5. 실제 운영 중인 앱만 — 컨셉·목업·미출시 서비스는 후보에서 제외한다. 모든 후보에 근거 URL(날짜 포함)을
     최소 1개 첨부한다.
  6. 기회 점수(opportunity_score) 산정 — 마켓별 병렬 조사(market-scout 방식) 후 기회 분석(gap-analyst 방식)으로
     0~100점을 매긴다. scope가 클수록 감점, 오래된 유행일수록 감점, 인디·빠른 MVP 가능성일수록 가점.

카피 리스크 판정 기준 (§12.3, 문구를 바꾸지 말고 그대로 적용):
  "기능·컨셉 참고는 가능 / 이름·아이콘·그래픽·문구·고유 브랜드 요소 모방은 금지."
  유사 상표명·명백한 명칭 도용은 후보 단계에서 자동 배제하고 표에 올리지 않는다.
-->

# IDEAS.md — 아이디어 후보 (기계용 메타)

## 실행 정보 *(소비: 재실행 시 회차 판단)*

- 실행 일시: {{run_datetime}}
- 실행 모드: {{run_mode}} <!-- "직접 입력" | "웹 리서치 제안" -->
- 최신 회차: {{round_no}} <!-- 재실행 시 이 번호로 기존 회차와 신규 회차를 구분한다 -->

## 후보 카드 목록 *(소비: PRD(2단계) 카드 선택 입력 · 릴리즈(7단계) §7 차별점 대조)*

| 회차 | slug | 한 줄 컨셉 | 타겟 | 시장방향 | 카테고리 | opportunity_score | 원본앱 (근거 URL · 확인 날짜) | 수익모델 | 개발 난이도(scope) | 카피 리스크 판정 | HTML 카드 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| {{round_no}} | {{slug_1}} | {{one_liner_1}} | {{target_summary_1}} | {{direction_1}} | {{category_1}} | {{opportunity_score_1}} | {{origin_app_1}} ([근거]({{evidence_url_1}}), {{evidence_date_1}}) | {{monetization_1}} | {{scope_1}} | {{copyright_verdict_1}} | `ideas/01-{{slug_1}}.html` |
| {{round_no}} | {{slug_2}} | {{one_liner_2}} | {{target_summary_2}} | {{direction_2}} | {{category_2}} | {{opportunity_score_2}} | {{origin_app_2}} ([근거]({{evidence_url_2}}), {{evidence_date_2}}) | {{monetization_2}} | {{scope_2}} | {{copyright_verdict_2}} | `ideas/02-{{slug_2}}.html` |
| {{round_no}} | {{slug_N}} | {{one_liner_N}} | {{target_summary_N}} | {{direction_N}} | {{category_N}} | {{opportunity_score_N}} | {{origin_app_N}} ([근거]({{evidence_url_N}}), {{evidence_date_N}}) | {{monetization_N}} | {{scope_N}} | {{copyright_verdict_N}} | `ideas/NN-{{slug_N}}.html` |

<!-- direction 값은 "US→KR" 또는 "KR→US"만 사용. 표에는 후보 풀(8~10개) 중 상위 3~5개만 카드로
     싣는다 — 카테고리 다양성 원칙·1/4 규칙은 후보 풀 전체를 기준으로 판단하고, 표는 그 결과로
     선별된 카드만 남긴다. 카드가 3개 미만이면 표를 줄이지 말고 실제 채운 행만 남기되, 6원칙
     미충족 사유를 "전역 중복 방지 대조 결과" 아래 한 줄로 덧붙인다. 재실행 시 "회차" 열로 이전
     회차 행을 구분해 보존한다(위 규칙 블록 참조). -->

## 선택 안내 *(소비: 사용자 폰 UI · PRD(2단계) 진입 트리거)*

사용자가 폰에서 각 카드를 열람한 뒤 채팅에 `{{slug}} 선택`이라고 보내면 해당 아이디어로 다음 단계(PRD)를 진행합니다.

- 직접 입력 모드({{run_mode}} = "직접 입력")인 경우: 후보 표는 1행만 채우고 위 리서치 6원칙·중복 대조는
  "해당 없음"으로 표시한다(사용자가 이미 아이디어를 확정했으므로 시장 조사 절차를 강제하지 않는다).

## 자세히(만든 과정 기록) *(소비: ideas-index.json 전역 감사 · 리서치 근거 검증)*

- 리서치 후보 풀 수: {{research_pool_count}} <!-- 8~10개. 카테고리 다양성 원칙·1/4 규칙은 이 풀 전체에 적용 -->
- 카드 제안 수: {{proposed_card_count}} <!-- 후보 풀 중 상위 3~5개만 표에 카드로 제시 -->
- US→KR 후보 수: {{count_us_to_kr}}
- KR→US 후보 수: {{count_kr_to_us}}
- 커버 카테고리 수: {{category_count}} (원칙: 최소 5개, 어느 카테고리도 전체 후보의 1/4 초과 금지 — 후보 풀 기준)
- 전역 중복 방지 대조 결과: {{dedup_note}} <!-- ideas-index.json과 대조해 컷된 후보 수/사유 요약. 컷 없으면 "해당 없음" -->

---

이 문서는 `templates/IDEAS.template.md`의 고정 블록(실행 정보 + 후보 카드 목록 + 선택 안내 +
자세히(만든 과정 기록))을 채운 결과물이다. 스킬은 섹션을 추가·삭제할 수 없고(해당 없으면 "해당
없음"을 이유와 함께 명시), 완료 시 `pipeline.json`의 `artifacts.ideas` 키에 이 파일의 상대 경로가
기록된다. 사용자가 채팅으로 `{{slug}} 선택`을 보내면 2단계(PRD)가 시작된다.
