<!--
이 템플릿을 채우는 스킬(Phase 4 `/pipeline-estimate`)을 위한 규칙

- 섹션 고정: 아래 "메타" 전문(前文) + §1~§7(백엔드 결정 트리 결과 / 기능→서비스 매핑 표 /
  비용 리스크 표 / MAU 시나리오별 예상 월 비용 / 개발 범위 요약 / 권장 구성 / PRD diff 요약),
  총 8개 블록의 제목·순서를 추가·삭제하지 않는다. 해당 내용이 없어도 섹션 자체를 지우지 말고
  "해당 없음"이라고 명시적으로 적는다 — 특히 백엔드 미채택(①로컬로 충분)이면 §3 비용 리스크 표와
  §4 MAU 시나리오는 "해당 없음 — 로컬 완결형, 서버 운영비 없음"으로 표기하고 표 자체는 지우지 않는다.
- 이 문서의 목표는 비용 "예측"이 아니라 **"월 운영비 ≈ 0인 아키텍처를 기본값으로 선택"**하는
  것이다. §1의 결정 트리는 반드시 ①→②→③ 순서로 적용하고, 상위 단계에서 답이 나오면 하위 단계는
  검토하지 않은 이유를 한 줄로 남긴다(건너뛴 게 아니라 판단한 것임을 남기기 위함).
- §1에서 백엔드(③)를 채택하면 기본 스택은 **Supabase**다(flutter-dev-harness `backend-builder`
  방법론 재사용): PRD §5 데이터 엔티티 표를 SQL 마이그레이션 + RLS 정책으로 옮기고, 인증 구성
  방식을 명시하고, anon key/service key를 분리하고, 자격증명은 `.env.example`로만 남기고
  실제 키는 커밋하지 않는다. Firebase는 특정 요건(예: RTDB 기반 version-check처럼 Supabase로
  어려운 경우)에만 대안으로 채택하고, 그 경우 §1과 §6에 근거를 명시한다. 비용 가드레일의 read
  최소화 원칙은 스택과 무관하게 동일하게 적용된다(`COST-GUARDRAILS.md`).
- §3 비용 리스크 표의 각 행은 **행동 단위 산식**으로 적는다. 형식 예시(그대로 따를 것):
  "피드 열 때마다 Firestore read 30건 × DAU 1천 × 10회/일 = 30만 read/일 → 무료 티어(5만/일)
  6배 초과". 산식 없이 "비용이 늘 수 있음" 같은 정성적 서술만 적는 것은 금지 — 반드시
  행동 × 빈도 × DAU = 일일 호출량 형태의 계산과, 무료 티어 대비 배율/초과 여부를 함께 적는다.
  각 행에는 완화책(COST-GUARDRAILS.md의 해당 항목을 인용)과 **완화 후 예상치**를 반드시 같이
  적는다 — 완화책만 적고 완화 후 수치를 비워두지 않는다.
- §4는 무료 티어 기준 + 1천 MAU + 1만 MAU, 최소 3개 시나리오의 예상 월 비용을 표로 남긴다.
- **목업 재동기화 규칙**: 이 단계의 기능 가감 조정이 PRD §4 화면 목록(화면 ID 추가/삭제/역할
  변경)에 영향을 주면 3단계(목업)로 자동 롤백한다 — 단, 변경된 화면만 재생성·재컨펌하고 나머지
  화면은 유지한다. 화면 ID 대조는 항상 **이 조정으로 갱신된 PRD**가 기준이다. §5에 롤백 필요
  여부를 명시할 것.
- **PRD 재승인 게이트**: "자동 갱신"이라도 무통지 변경은 금지한다 — §7 PRD diff 요약에 이번
  조정으로 PRD §3(기능 목록)·§4(화면 목록)가 어떻게 바뀌었는지 표시하고, 이 산정 문서에 대한
  사용자 컨펌은 곧 **갱신된 PRD에 대한 재승인**으로 정의한다(화면에 영향 없는 기능 가감이라도
  사용자가 승인한 PRD가 몰래 바뀌지 않게 하기 위함). 조정이 전혀 없었다면 §7에 "해당 없음 —
  PRD 대비 기능 가감 없음"이라고 적는다.
- 완료 시 pipeline.json 등록: 이 파일의 프로젝트 내 상대 경로(`ESTIMATE.md`, 프로젝트 루트 —
  `docs/` 하위 아님)를 `artifacts.estimate` 키로 기록한다.
- 원자적 쓰기: 임시 파일에 쓴 뒤 rename으로 교체한다. 쓰는 도중의 부분 파일 상태를 그대로 남기지
  않는다.
-->

# ESTIMATE — {{app_title}}

> 4단계 산출물(`ESTIMATE.md`, 프로젝트 루트). 3단계 목업 컨펌 후 진입. 폰에서는 필요 서비스
> 요약 + MAU 시나리오별 예상 월 비용 카드로 보이고, 기능 가감을 채팅 피드백으로 조정한 뒤
> 컨펌하면 5단계(개발)가 열린다 — 이 컨펌은 §7에 표시된 PRD 변경사항에 대한 재승인이기도 하다.

## 메타

- **slug**: {{slug}}
- **버전**: {{version}} <!-- 예: v1.0. 조정할 때마다 올리지 않아도 되며, 컨펌 시점 버전만 갱신 -->
- **상태**: {{status}} <!-- 초안 | 조정 중 | 컨펌 대기 | 컨펌됨 -->
- **기준 PRD 버전**: {{prd_version_ref}} <!-- 이 산정이 어느 PRD 버전을 기준으로 이뤄졌는지 -->
- **조정 회차**: {{revision_no}} <!-- 최초 산정 = 1, 기능 가감으로 재조정할 때마다 증가 -->

## §1 백엔드 결정 트리 결과 *(소비: 개발(5단계 기술 스택 결정) · PRD §8 백엔드 결정 트리 결과와 동일 값이어야 함)*

| 단계 | 질문 | 판단 | 근거 |
|---|---|---|---|
| ① 로컬 | 이 기능이 기기 로컬 저장만으로 되는가? | {{step1_answer}} <!-- 예/아니오 --> | {{step1_reason}} |
| ② 정적/번들 | (①이 아니오일 때) 서버가 필요해도 정적 데이터로 대체 가능한가? | {{step2_answer}} <!-- 예/아니오/해당 없음(①에서 확정) --> | {{step2_reason}} |
| ③ 백엔드 | (②도 아니오일 때) 진짜 사용자 간 공유·동기화가 필요한가? | {{step3_answer}} | {{step3_reason}} — 이 기능이 수익모델(PRD §6)을 정당화하는가: {{step3_monetization_justification}} |

- **최종 결과**: {{backend_decision_final}} <!-- 예: ①에서 확정 — 백엔드 없음(전 기능 로컬) /
  ②에서 확정 — 정적 번들·호스팅 / ③ 채택 — Supabase 기본, 근거: {{backend_reason_summary}} /
  ③ 채택 — Firebase(대안), 근거: {{alt_stack_reason}} -->
- **스택 상세** (백엔드 채택 시에만 작성, 아니면 "해당 없음"): {{backend_stack_detail}}
  <!-- 예: Supabase — 인증(이메일+익명), RLS로 사용자별 행 접근 제한, anon key는 클라이언트,
  service key는 서버 전용, 자격증명은 .env.example로 분리(커밋 금지) -->

<!-- 템플릿 규칙 — ①→②→③ 순서로 반드시 채운다. 상위 단계에서 답이 나오면 하위 단계 판단칸에
     "해당 없음(①/②에서 확정)"이라고 적어 검토 자체는 했음을 남긴다. -->

## §2 기능→서비스 매핑 표 *(소비: 개발(5단계 의존성 설치·설정))*

| 기능 (PRD §3 기준) | 필요 서비스 | 스택/플러그인 | 비고 |
|---|---|---|---|
| {{feature_1}} | {{feature_1_service}} <!-- 예: 인증, DB, 스토리지, 광고, 없음(로컬) --> | {{feature_1_stack}} | {{feature_1_note}} |
| {{feature_n}} | {{feature_n_service}} | {{feature_n_stack}} | {{feature_n_note}} |

<!-- 백엔드 미채택이면 이 표의 "필요 서비스" 열은 전부 "없음(로컬)"로 채우고 표는 유지한다. -->

## §3 비용 리스크 표 *(소비: 개발(5단계 가드레일 적용) · 다음 조정 라운드)*

<!-- 백엔드 미채택이면 이 섹션 전체를 "해당 없음 — 로컬 완결형, 서버 운영비 없음"으로 표기하고
     아래 표는 헤더만 남기거나 삭제하지 않고 그대로 둔다. -->

| 사용자 행동 | 산식 (행동 × 빈도 × DAU = 일일 호출) | 무료 티어 대비 | 완화책 | 완화 후 예상치 |
|---|---|---|---|---|
| {{risk_1_action}} <!-- 예: 피드 열기 --> | {{risk_1_formula}} <!-- 예: Firestore read 30건 × DAU 1천 × 10회/일 = 30만 read/일 --> | {{risk_1_vs_free_tier}} <!-- 예: 무료 티어(5만/일) 6배 초과 --> | {{risk_1_mitigation}} <!-- 예: COST-GUARDRAILS.md 체크리스트 항목: 로컬 캐시+TTL 우선, 집계 문서(counter) 패턴 --> | {{risk_1_after_mitigation}} <!-- 예: 캐시 적용 후 일 5만 read → 무료 티어 이내 --> |
| {{risk_n_action}} | {{risk_n_formula}} | {{risk_n_vs_free_tier}} | {{risk_n_mitigation}} | {{risk_n_after_mitigation}} |

<!-- 템플릿 규칙 — 산식 없는 정성적 서술("비용이 늘 수 있다" 등)만 적는 것 금지. 완화책 열과
     완화 후 예상치 열은 항상 짝으로 채운다(완화책만 적고 예상치를 비워두지 않는다). -->

## §4 MAU 시나리오별 예상 월 비용 *(소비: PRD §7 성공 지표의 수익 추정 검증 · 다음 조정 라운드)*

<!-- 백엔드 미채택이면 "해당 없음 — 서버 운영비 없음"으로 표기. -->

| 시나리오 | 무료 티어 초과 여부 | 예상 월 비용 | 근거 |
|---|---|---|---|
| 무료 티어 (초기) | {{scenario_free_over_limit}} | {{scenario_free_cost}} <!-- 예: $0 --> | {{scenario_free_basis}} |
| 1천 MAU | {{scenario_1k_over_limit}} | {{scenario_1k_cost}} | {{scenario_1k_basis}} |
| 1만 MAU | {{scenario_10k_over_limit}} | {{scenario_10k_cost}} | {{scenario_10k_basis}} |

## §5 개발 범위 요약 *(소비: 개발(5단계 착수 기준))*

{{scope_summary}}
<!-- 템플릿 규칙 — 이번 산정에서 기능을 뺐다면(WON'T v1 이동) 무엇을·왜, 넣었다면 무엇을·왜
     적는다. 목업 재동기화 규칙: 이 조정이 화면 목록(화면 ID 추가/삭제/역할 변경)에 영향을 줬는지
     아래에 명시한다. -->

- **목업 롤백 필요 여부**: {{mockup_rollback_needed}} <!-- 예: 필요 없음(화면 변화 없음) /
  필요 — 영향받은 화면: `{{affected_screen_ids}}`(해당 화면만 재생성·재컨펌, 나머지 화면은 기존
  목업 유지) -->

## §6 권장 구성 *(소비: 개발(5단계 초기 설정))*

- **런타임/서비스**: {{recommended_stack_summary}} <!-- 예: Supabase 프로젝트 1개, 테이블 3개,
  Auth(이메일), Storage 버킷 1개 -->
- **환경 분리**: {{env_separation_plan}} <!-- 예: anon key만 앱에 포함, service key는 Edge
  Function 전용, .env.example로 자격증명 형식만 커밋 -->
- **마이그레이션/RLS 개요**: {{migration_rls_summary}} <!-- 백엔드 미채택이면 "해당 없음" -->
- **비용 가드레일 적용 대상**: `COST-GUARDRAILS.md` 참조 — 이 프로젝트의 가드레일 적용 범위:
  {{cost_guardrail_applicability}} <!-- 예: 전체 6항목 + Supabase 대응 3항목 적용 / 해당 없음
  (백엔드 미채택) -->

## §7 PRD diff 요약 *(재승인 게이트 — 소비: 사용자 컨펌 카드)*

<!-- 이번 조정이 없었다면 "해당 없음 — PRD 대비 기능 가감 없음"이라고 적는다. 조정이 있었다면
     아래 표를 채운다. 이 산정 문서의 컨펌은 곧 갱신된 PRD의 재승인으로 정의된다(무통지 변경
     금지). -->

| 변경 대상 | 변경 전 (PRD {{prd_version_ref}}) | 변경 후 | 사유 |
|---|---|---|---|
| {{diff_1_target}} <!-- 예: §3 기능 목록 — MUST 기능 X --> | {{diff_1_before}} | {{diff_1_after}} | {{diff_1_reason}} |
| {{diff_n_target}} | {{diff_n_before}} | {{diff_n_after}} | {{diff_n_reason}} |

---

이 문서는 `templates/ESTIMATE.template.md`의 고정 블록(메타 + §1~§7)을 채운 결과물이다. 스킬은
섹션을 추가·삭제할 수 없고(해당 없으면 "해당 없음"을 이유와 함께 명시), 컨펌 시 필수 섹션 존재가
자동 검증되어 `pipeline.json`에 기록된다. §7에 표시된 PRD 변경사항은 이 문서 컨펌과 동시에
재승인된 것으로 간주되며, 화면 목록에 영향이 있었다면 3단계(목업)로 롤백되어 변경된 화면만
재컨펌을 거친다.
