<!--
이 템플릿을 채우는 스킬(Phase 4 `/pipeline-release`)을 위한 규칙

- 섹션 고정: 아래 "메타" 전문(前文) + §1~§8(Play 콘솔 입력란 / App Store Connect 입력란 /
  심사 설문 답변 가이드 / 개인정보처리방침 / 릴리즈 노트 / 스토어 자산 매니페스트 / 이전 출시
  앱과의 차별점 / 최종 체크리스트), 총 9개 블록의 제목·순서를 추가·삭제하지 않는다. 해당 내용이
  없어도 섹션 자체를 지우지 말고 "해당 없음"이라고 이유와 함께 명시한다.
- **글자수 제한은 절대값이다** — `commands/skills/app_release_info/SKILL.md`의 규칙과 반드시
  일치해야 한다: Play 앱 이름 30자 이하 · 짧은 설명 80자 이하 · 전체 설명 4000자 이하 / App Store
  이름 30자 이하 · 부제 30자 이하 · 설명 4000자 이하 · 키워드 100자 이하. 아래 각 입력란은
  `(현재 글자수/제한)` 형식의 자가 카운트 칸을 두며, 채울 때 실제로 글자수를 세어 초과 시 그
  자리에서 줄인다 — 초과된 채로 다음 단계에 넘기지 않는다. 앱 이름·설명 등 스토어 텍스트는
  app_release_info 스킬 규칙대로 **영어로** 작성하고(다국어 지원 시 §5에 추가 로케일), 특수문자
  사용을 금지한다.
- **스토어 콘솔 입력란과 1:1 순서(복붙 동선)**: §1 Play, §2 App Store 모두 실제 콘솔에 입력하는
  순서 그대로 행을 배치한다 — Play: 앱 이름 → 짧은 설명 → 전체 설명 → 카테고리·태그 → 스크린샷
  목록 → 피처 그래픽 → 512 아이콘. App Store: 이름 → 부제 → 설명 → 키워드 → 카테고리 → 스크린샷 →
  1024 아이콘. 이 순서를 스킬이 임의로 재배열하지 않는다(사용자가 위에서 아래로 그대로 복붙하도록).
- 스토어 자산 규격은 `commands/app-store-screenshots/reference/store-specs.md` 기준(2026년
  현재값)을 그대로 인용한다: Android 아이콘 512×512(32-bit PNG, 알파 허용) · 피처 그래픽
  1024×500(알파 없음) · 폰 스크린샷 1080×1920 권장(최소 2장, 최대 8장) / iOS 아이콘
  1024×1024(PNG/JPEG, RGB, **알파 없음**) · 스크린샷은 지원 기기별 필수 해상도(예: iPhone 6.9"
  1320×2868) 최소 1장~최대 10장. 규격 수치가 바뀌면 이 템플릿이 아니라 store-specs.md를 갱신하고
  이 문서는 그 파일을 참조만 한다.
- §3 심사 설문 표는 앱 구성(광고 채택 여부 · 로그인 채택 여부 · 백엔드 채택 여부 — ESTIMATE.md §1·
  §2, PRD.md §6·§8 참조)에 따라 권장 답변이 달라진다. 광고 스킬(interstitial-splash-ad,
  reward-ads 등)을 채택했으면 Play 데이터 안전 섹션의 "광고 ID 수집"은 **자동으로 "예"**다 —
  이 값을 스킬이 임의로 "아니요"로 적지 않는다.
- §4 개인정보처리방침은 텍스트 본문 + **호스팅 URL 기록란**으로 구성한다. **터널 URL(ngrok 등)·
  PC 로컬호스트 URL은 절대 기록하지 않는다** — 스토어는 심사·출시 후에도 상시 접근 가능한 URL을
  요구하므로, 정본은 반드시 GitHub Pages로 발행한 URL(`https://<계정>.github.io/<repo>/...` 형태)
  이어야 한다. gh CLI 인증이 안 되어 있으면 이 섹션에 "미발행 — gh CLI 인증 필요"라고 명시하고
  체크리스트(§8)에서 미완료로 남긴다.
- §7 이전 출시 앱과의 차별점은 §12.3(Google Play 반복적 콘텐츠/spam 정책) 대응이다 — 플랫폼으로
  여러 앱을 계속 찍어내는 사용자에게 실질적 차별점을 요구하는 경고 문구를 반드시 포함하고,
  `ideas-index.json`에 기록된 과거 출시작과 비교해 채운다. 차별점이 빈약하면(카테고리·핵심기능이
  거의 동일) 스킬은 경고를 그대로 노출해야 하며 임의로 문구를 순화하지 않는다.
- §8 최종 체크리스트는 스펙 §2-7단계·§2.5 RELEASE 행·app_release_info 스킬의 글자수 규칙과
  모순이 없는지 항목별로 확인하는 게이트다 — 체크되지 않은 항목이 있으면 릴리즈 컨펌을 막는다.
- 완료 시 pipeline.json 등록: 이 파일은 프로젝트 루트 `release/RELEASE.md`로 저장하고
  `artifacts.release: "release/"`(디렉터리 전체 — 스크린샷·아이콘·개인정보처리방침 사본 등
  동반 파일 포함)로 기록한다.
- 원자적 쓰기: 임시 파일에 쓴 뒤 rename으로 교체한다. 쓰는 도중의 부분 파일 상태를 그대로 남기지
  않는다.
-->

# RELEASE — {{app_title}}

> 7단계 산출물(`release/RELEASE.md` + 동반 자산, 프로젝트 루트 `release/` 디렉터리). 6단계(테스트)
> 컨펌 후 진입. `/app-store-screenshots`로 캡처한 스크린샷과 `app_release_info`로 생성한 스토어
> 텍스트를 이 문서 한 곳에 정리해, 폰에서 갤러리+텍스트를 확인하고 컨펌하면 사용자는 Play Console /
> App Store Connect에 **복붙만 하면 되는 상태**로 종료된다.

## 메타

- **slug**: {{slug}}
- **버전**: {{version}} <!-- 예: v1.0 (릴리즈 노트의 버전과 일치) -->
- **상태**: {{status}} <!-- 초안 | 자산 생성 중 | 컨펌 대기 | 컨펌됨 -->
- **기준 PRD 버전**: {{prd_version_ref}}
- **기준 ESTIMATE 버전**: {{estimate_version_ref}}
- **앱 구성 요약** *(§3 설문 답변·§7 차별점 판단의 입력)*: 광고 {{ads_adopted}} <!-- 예/아니요 --> ·
  로그인 {{login_adopted}} <!-- 예/아니요 --> · 백엔드 {{backend_adopted}} <!-- 예(Supabase 등)/아니요 -->

## §1 Google Play Console 입력란 *(콘솔 순서 그대로 — 복붙 동선)*

| 순서 | 입력란 | 값 | 글자수 |
|---|---|---|---|
| 1 | 앱 이름 (App name) | {{play_title}} <!-- 형식: {앱이름} - {검색 키워드 설명}, 특수문자 금지 --> | {{play_title_char_count}}/30 |
| 2 | 짧은 설명 (Short description) | {{play_short_desc}} | {{play_short_desc_char_count}}/80 |
| 3 | 전체 설명 (Full description) | {{play_full_desc}} <!-- 구조: 핵심가치 문단 → bullet 주요기능 → 사용 시나리오 → CTA --> | {{play_full_desc_char_count}}/4000 |
| 4 | 카테고리 (Category) | {{play_category}} | — |
| 4 | 태그 (Tags, 5~8개) | {{play_tags}} | — |
| 5 | 스크린샷 목록 (Phone, 1080×1920 권장) | {{play_screenshot_manifest_ref}} <!-- §6 자산 매니페스트 표 참조, 최소 2장·최대 8장 --> | 최소 2 / 최대 8장 |
| 6 | 피처 그래픽 (Feature graphic, 1024×500) | `{{play_feature_graphic_path}}` | 1024×500, 알파 없음 |
| 7 | 앱 아이콘 (512×512) | `{{play_icon_512_path}}` | 512×512, 32-bit PNG(알파 허용) |

<!-- 템플릿 규칙 — 이 표의 행 순서는 Play 콘솔 스토어 등록정보 화면 진행 순서와 1:1이다. 스킬은
     이 순서를 재배열하지 않는다. 글자수 칸이 제한을 넘으면 값 칸을 그 자리에서 줄인다. -->

## §2 App Store Connect 입력란 *(콘솔 순서 그대로 — 복붙 동선)*

| 순서 | 입력란 | 값 | 글자수 |
|---|---|---|---|
| 1 | 이름 (Name) | {{ios_title}} <!-- 형식: {앱이름} - {검색 키워드 설명}, 특수문자 금지 --> | {{ios_title_char_count}}/30 |
| 2 | 부제 (Subtitle) | {{ios_subtitle}} | {{ios_subtitle_char_count}}/30 |
| 3 | 설명 (Description) | {{ios_full_desc}} <!-- 구조는 Play 전체 설명과 동일 원칙 --> | {{ios_full_desc_char_count}}/4000 |
| 4 | 키워드 (Keywords, 쉼표 구분) | {{ios_keywords}} <!-- 앱 이름에 이미 포함된 단어 제외 --> | {{ios_keywords_char_count}}/100 |
| 5 | 카테고리 (Primary / Secondary) | {{ios_category_primary}} / {{ios_category_secondary}} | — |
| 6 | 스크린샷 목록 (기기별 필수 해상도) | {{ios_screenshot_manifest_ref}} <!-- §6 자산 매니페스트 표 참조 --> | 기기 클래스당 1~10장 |
| 7 | 앱 아이콘 (1024×1024) | `{{ios_icon_1024_path}}` | 1024×1024, PNG/JPEG RGB, **알파 없음** |

<!-- 템플릿 규칙 — Play와 App Store의 값이 같은 기능을 설명하더라도 글자수 제한이 다르므로
     (이름 30 vs 30 동일, 설명 4000 vs 4000 동일이나 Play엔 짧은 설명 80자가 별도 존재) 각각
     독립적으로 글자수를 세고 표시한다. 두 섹션의 값을 기계적으로 복사-붙여넣기만 하지 않는다
     (Play 짧은 설명은 App Store에 대응 입력란이 없음에 유의). -->

## §3 심사 설문 답변 가이드 *(소비: 사용자가 Play/App Store 콘솔에서 그대로 선택)*

### 3-1 Google Play — 데이터 안전 섹션 + 콘텐츠 등급

| 앱 구성 | 광고 ID 수집 | 개인정보(이메일 등) 수집 | 위치정보 수집 | 데이터 암호화 전송 | 계정삭제 방법 제공 |
|---|---|---|---|---|---|
| 광고 없음 · 로그인 없음 · 백엔드 없음 | 아니요 | 아니요 | 아니요 | 해당 없음 | 해당 없음(계정 자체 없음) |
| 광고 있음 · 로그인 없음 · 백엔드 없음 | **예**(광고 SDK가 광고 ID 수집) | 아니요 | 아니요 | 해당 없음 | 해당 없음(계정 자체 없음) |
| 광고 없음 · 로그인 있음 · 백엔드 있음 | 아니요 | 예(이메일/식별자) | {{location_collect_answer}} <!-- 기능에 위치 사용 시만 예 --> | 예(HTTPS/TLS) | **예 — 앱 내 계정 삭제 화면 제공**(URL: {{account_deletion_url_or_screen}}) |
| 광고 있음 · 로그인 있음 · 백엔드 있음 | **예**(광고 SDK가 광고 ID 수집) | 예(이메일/식별자) | {{location_collect_answer}} | 예(HTTPS/TLS) | **예 — 앱 내 계정 삭제 화면 제공**(URL: {{account_deletion_url_or_screen}}) |
| 광고 무관 · 로그인 없음 · 백엔드 있음 | (규칙 적용 아래 참조) | 아니요 | 아니요 | 예(HTTPS/TLS) | 해당 없음(계정 자체 없음) |

- **이 프로젝트의 실제 구성 기준 최종 답변**: {{play_data_safety_final_summary}} <!-- 위 표에서
  메타의 앱 구성(광고/로그인/백엔드)에 해당하는 행을 인용, 정확히 일치하는 행이 없으면 아래 도출 규칙으로 조합해 확정 -->

**속성별 도출 규칙**: 위 표에 정확히 일치하는 행이 없으면 다음 규칙으로 조합한다:
- **광고 있음**: 광고 ID 수집 "예"(광고 SDK 정책에 따름)
- **광고 없음**: 광고 ID 수집 "아니요", 광고 관련 모든 항목 제외
- **로그인 있음**: 개인정보(이메일 등) 수집 "예" + 계정삭제 기능 제공 "예"
- **로그인 없음**: 개인정보 수집 "아니요", 계정삭제 "해당 없음"
- **백엔드 있음**: 데이터 암호화 전송 "예(HTTPS/TLS)", 앱 기능 데이터 수집 포함
- **백엔드 없음**: 데이터 전송 없음, 기기 로컬 저장만 해당
- 각 속성은 독립적으로 적용되며 조합된다.
- **콘텐츠 등급 설문(IARC)**: 폭력·도박·성인 콘텐츠·사용자 생성 콘텐츠 없는 일반 유틸리티/생산성
  앱 기준 원칙 = 전 항목 "아니요", 인앱 광고 존재 시 "인앱 광고 있음"만 "예"로 표기. 이 원칙과
  다른 특수 콘텐츠가 있으면 여기에 근거와 함께 명시: {{content_rating_deviation}} <!-- 없으면
  "해당 없음 — 위 원칙 그대로 적용" -->

### 3-2 App Store — 개인정보 (Privacy) 라벨

| 앱 구성 | Data Used to Track You | Data Linked to You | Data Not Linked to You |
|---|---|---|---|
| 광고 없음 · 로그인 없음 · 백엔드 없음 | 없음 | 없음 | 없음(또는 진단 데이터만) |
| 광고 있음 · 로그인 없음 · 백엔드 없음 | 광고 데이터(광고 SDK 정책에 따름 — SDK 문서 확인 필수) | 없음 | 사용 데이터(광고 식별자) |
| 광고 없음 · 로그인 있음 · 백엔드 있음 | 없음 | 사용자 ID·이메일 등 계정 식별자 | 진단 데이터 |
| 광고 있음 · 로그인 있음 · 백엔드 있음 | 광고 데이터 | 사용자 ID·이메일 등 계정 식별자 | 사용 데이터 + 진단 데이터 |
| 광고 무관 · 로그인 없음 · 백엔드 있음 | (규칙 적용 아래 참조) | 없음 | 진단 데이터(선택사항) |

- **이 프로젝트의 실제 구성 기준 최종 답변**: {{ios_privacy_label_final_summary}} <!-- 채택한
  광고 SDK의 실제 프라이버시 선언(App Store Connect의 "제3자 SDK 프라이버시" 섹션)을 반드시 대조
  후 확정 — 이 표는 출발점이지 최종 확정값이 아니다. 정확히 일치하는 행이 없으면 아래 도출 규칙으로 조합 -->

**속성별 도출 규칙**: 위 표에 정확히 일치하는 행이 없으면 다음 규칙으로 조합한다:
- **광고 있음**: "Data Used to Track You"에 광고 데이터 포함(광고 SDK 프라이버시 정책 기준)
- **광고 없음**: "Data Used to Track You" 없음, 광고 관련 모든 항목 제외
- **로그인 있음**: "Data Linked to You"에 사용자 ID·이메일 등 계정 식별자 포함
- **로그인 없음**: "Data Linked to You" 없음, 계정 관련 정보 제외
- **백엔드 있음**: 앱 기능 데이터 수집/전송(구체 항목은 PRD 데이터 모델 참고)
- **백엔드 없음**: 데이터 전송 없음, 기기 로컬 저장만 해당
- 각 속성은 독립적으로 적용되며 조합된다.

<!-- 템플릿 규칙 — 광고 스킬을 채택했으면 "광고 ID 수집"/추적 관련 항목은 이 템플릿이 자동으로
     "예"/"있음" 방향으로 채운다(스펙 §2-7). 로그인을 채택했으면 계정삭제 항목은 반드시 "예"이며
     실제 URL/화면 경로를 비워두지 않는다(PRD §3 Must에 계정삭제 기능이 이미 반영되어 있어야 함). -->

## §4 개인정보처리방침 *(소비: Play/App Store 심사 필수 제출물)*

- **본문 요약 구성**: 수집하는 데이터 종류(§3 설문 답변과 일치해야 함) / 수집 목적 / 제3자 제공
  (광고 SDK명 명시) / 보유·파기 기준 / 문의처(이메일) / 시행일. 앱 이름 외의 실제 개인 식별 정보는
  방침 본문에 넣지 않는다(§12.2 — 공개 게시 문서이므로).
- **본문 초안**: {{privacy_policy_draft}} <!-- 위 구성 요소를 채운 전문. 광고/로그인/백엔드 유무에
  따라 §3의 최종 답변과 문구가 어긋나지 않게 작성 -->
- **호스팅 URL**: {{privacy_policy_url}} <!-- 반드시 GitHub Pages 발행 URL
  (https://<계정>.github.io/<repo>/... 형태). ngrok 등 터널 URL·localhost·사설 IP는 절대 기록
  금지 — 스토어 심사·출시 후 상시 접근 가능해야 하기 때문 -->
- **발행 상태**: {{privacy_policy_publish_status}} <!-- 예: 발행 완료(gh CLI 인증됨, 위 URL로 접근
  확인함) / 미발행 — gh CLI 인증 필요(사유: {{privacy_policy_publish_blocker}}) -->

## §5 릴리즈 노트 *(소비: Play "이번 업데이트의 새로운 기능" · App Store "What's New")*

- **버전**: {{release_notes_version}} <!-- 메타의 버전과 동일 -->
- **노트 본문(영어, 특수문자 최소화)**:
  - {{release_note_item_1}} <!-- e.g., New: Added drag-to-reorder in the habit list -->
  - {{release_note_item_n}}
- **추가 로케일**: {{release_notes_locales}} <!-- 다국어 지원 앱이면 로케일별 노트 목록, 아니면
  "해당 없음(영어 단일)" -->

## §6 스토어 자산 매니페스트 *(소비: 폰 스크린샷 갤러리 · 콘솔 업로드 순서 §1/§2와 대응)*

| 슬라이드 번호 | 자산 | 경로 | 규격 (store-specs.md 기준) | PRD §4 화면 ID 대응 |
|---|---|---|---|---|
| — | Android 아이콘 | `release/icons/play-512.png` | 512×512, 32-bit PNG(알파 허용) | — |
| — | iOS 아이콘 | `release/icons/ios-1024.png` | 1024×1024, PNG/JPEG RGB(알파 없음) | — |
| — | Play 피처 그래픽 | `release/graphics/feature-graphic.png` | 1024×500(알파 없음) | — |
| {{slide_no}} | Android 스크린샷 {{n}} | `release/screenshots/android/{{screen_id}}.png` | 1080×1920 권장 | `{{screen_id}}` |
| {{slide_no}} | iOS 스크린샷 {{n}} | `release/screenshots/ios/{{screen_id}}.png` | 기기 클래스별 필수 해상도(예: 6.9" 1320×2868) | `{{screen_id}}` |
| {{slide_no}} | iPad 스크린샷 {{n}} (iPad 지원 앱만 — 반복 패턴, iPad 미지원이면 이 행 자체를 생략) | `release/screenshots/ipad/{{screen_id}}.png` | 2064×2752 | `{{screen_id}}` |

<!-- 템플릿 규칙 — "슬라이드 번호"(01~)는 스토어 업로드 순서의 정본이다: 스크린샷은 이 번호 순서
     그대로 Play/App Store 콘솔에 업로드한다(아이콘·피처 그래픽은 업로드 순서 개념이 없으므로 "—").
     "PRD §4 화면 ID 대응" 열의 screen_id는 PRD.template.md §4 화면 목록의 화면 ID·스토어 스샷 열과
     동일해야 한다(관통 키). 스크린샷이 필요 없다고 PRD에 표시된 화면은 이 표에도 포함하지 않는다. -->

## §7 이전 출시 앱과의 차별점 *(§12.3 — Google Play 반복적 콘텐츠/spam 정책 대응, 필수)*

> **경고**: 같은 템플릿·구조로 앱을 계속 찍어내면서 실질적 차별성이 없으면 Google Play "반복적/
> 저품질 콘텐츠(spam)" 정책 위반으로 **계정 전체가 제재**될 수 있다. 이 섹션은 형식적으로 채우지
> 말고, 실제로 이전 출시작과 무엇이 다른지 근거를 남긴다.

- **`ideas-index.json` 기준 동일/유사 카테고리 과거 출시작**: {{prior_similar_apps}} <!-- 없으면
  "해당 없음 — 이 카테고리 최초 출시" -->
- **이번 앱의 실질적 차별점**: {{differentiation_summary}} <!-- 핵심 기능·데이터·타겟이 이전
  출시작과 구체적으로 어떻게 다른지. "디자인만 다름" 같은 답변은 불충분으로 간주 -->
- **판정**: {{spam_risk_judgment}} <!-- 예: 차별점 충분 — 진행 / 차별점 미흡 — 사용자에게 경고
  후 진행 여부 확인 필요 -->

## §8 최종 체크리스트 *(컨펌 게이트 — 전 항목 체크 전 릴리즈 컨펌 불가)*

- [ ] 테스트 AdMob 광고 유닛 ID → 실제 유닛 ID로 전량 교체 확인(interstitial-splash-ad/reward-ads
  채택 시에만 해당, 미채택이면 "해당 없음") — 확인 결과: {{checklist_admob_ids_result}}
- [ ] 로그인 채택 시: 앱 내 계정 삭제 기능이 실제로 동작하는지 확인 + §3-1 표의 URL/화면 경로가
  실제 존재(로그인 미채택이면 "해당 없음") — 확인 결과: {{checklist_account_deletion_result}}
- [ ] §7 판정이 "차별점 충분 — 진행"인지 확인(미흡 판정이면 이 항목은 체크하지 않고 릴리즈를
  보류) — 확인 결과: {{checklist_differentiation_result}}
- [ ] `release/icons/play-512.png` 파일이 실제로 존재하고 512×512인지 확인 — 확인 결과:
  {{checklist_icon_512_result}}
- [ ] `release/icons/ios-1024.png` 파일이 실제로 존재하고 1024×1024, 알파 없음인지 확인 —
  확인 결과: {{checklist_icon_1024_result}}
- [ ] §1·§2의 모든 글자수 칸이 제한 이내인지 재확인(app_release_info 스킬 규칙과 대조) —
  확인 결과: {{checklist_char_limits_result}}
- [ ] §4 호스팅 URL이 GitHub Pages 주소이고 터널/로컬 URL이 아닌지, 실제로 브라우저에서 접근
  확인 — 확인 결과: {{checklist_privacy_url_result}}
- [ ] §3 설문 답변이 실제 앱 구성(광고/로그인/백엔드)과 모순 없는지, 개인정보처리방침 본문과도
  일치하는지 확인 — 확인 결과: {{checklist_data_safety_consistency_result}}

---

이 문서는 `templates/RELEASE.template.md`의 고정 블록(메타 + §1~§8)을 채운 결과물이다. 스킬은
섹션을 추가·삭제할 수 없고(해당 없으면 "해당 없음"을 이유와 함께 명시), §8 전 항목이 체크되어야
사용자 컨펌이 가능하다. 컨펌 시 `pipeline.json`의 `stage`가 `done`으로 확정되며, 이후 사용자는
Play Console / App Store Connect에 §1·§2 값을 순서대로 복붙하고 §6 자산을 업로드하는 것으로
출시를 마무리한다.
